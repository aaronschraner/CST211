/********************************************************************************
 * T Dequeue ();
 * 	Purpose: 
 * 		Similar to Pop() in a stack but reversed,
 * 		Removes the first-added element (the head) of the stack and
 * 		returns a copy of it.
 * 	
 * 	Entry: 
 * 		TODO: change "stack" to "queue"
 * 		nothing
 * 	
 * 	Exit: 
 * 		Returns the element that was just removed
 * 	
 ********************************************************************************/
